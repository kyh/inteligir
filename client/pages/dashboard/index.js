import React from 'react';
import Head from 'next/head';
import Logo from '@client/components/Logo';

const Dashboard = () => {
  return (
    <section>
      <Head>
        <title>Inteligir | Dashboard</title>
      </Head>
      <Logo />
    </section>
  );
};

export default Dashboard;
