{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "prompt": "inteligir is driven by coding agents — read AGENTS.md first. The product is ONE Cloudflare Worker (apps/web); `pnpm dev:web` runs all of it locally on :5174 (pinned). `pnpm dev:desktop` runs the Electron SHELL — a window on that URL, nothing more. Gate with `pnpm format:fix && pnpm verify` — format:fix runs BEFORE the gates, never after (a post-gate run corrupts the byte-pinned round-trip fixtures). Never run `db:push` or `db:studio`: both target the PRODUCTION D1. The local one is `db:push:local`.",
  "permissions": {
    "allow": [
      "Bash(pnpm install)",
      "Bash(pnpm verify:*)",
      "Bash(pnpm typecheck:*)",
      "Bash(pnpm lint:*)",
      "Bash(pnpm format:*)",
      "Bash(pnpm test:*)",
      "Bash(pnpm build:*)",
      "Bash(pnpm knip:*)",
      "Bash(pnpm dev:desktop)",
      "Bash(pnpm dev:web)",
      "Bash(pnpm --filter @repo/web dev)",
      "Bash(pnpm --filter @repo/web test)",
      "Bash(pnpm --filter @repo/web db:push:local)",
      "Bash(agent-browser:*)"
    ],
    "deny": [
      "Bash(pnpm --filter @repo/web db:push)",
      "Bash(pnpm -F @repo/web db:push)",
      "Bash(pnpm --filter @repo/web db:studio)",
      "Bash(pnpm -F @repo/web db:studio)"
    ]
  }
}
