{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // the harness derives its deadlines from apps/cli's server modules, which target ES2024.
    "lib": ["ES2024"],
    "types": ["node"]
  },
  "include": ["src"]
}
