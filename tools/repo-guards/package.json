{
  "name": "@repo/repo-guards",
  "version": "0.1.0",
  "private": true,
  "description": "Derived fitness tests over the repository itself — the checks that belong to no product package because they read the whole tree: the CLAUDE.md dep-DAG paragraph against the real manifests (which also pins packages/ shipped source free of node/electron), the Bridge registry's freedom from dead channels, the pi harness quarantine, where `ssr` may be declared in apps/web's route tree, what the agent image's install layer is keyed on, and every relative markdown link resolving to a file that exists. Ships nothing. See README.md.",
  "type": "module",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo node_modules",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/bridge": "workspace:*",
    "@repo/notes": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
