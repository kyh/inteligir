{
  "name": "@repo/repo-guards",
  "version": "0.1.0",
  "private": true,
  "description": "Derived fitness tests over the repository itself — the checks that belong to no product package because they read the whole tree: the CLAUDE.md dep-DAG paragraph against the real manifests, the client packages' freedom from node/electron, and the Bridge registry's freedom from dead channels. Ships nothing.",
  "type": "module",
  "scripts": {
    "clean": "git clean -xdf .cache .turbo node_modules",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@repo/bridge": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
