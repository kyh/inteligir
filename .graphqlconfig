{
  "schemaPath": "server/schema/generated/schema.graphql",
  "extensions": {
    "endpoints": {
      "dev": {
        "url": "http://localhost:5000/graphql",
        "headers": {
          "Authorization": "Bearer ${env:AUTH_TOKEN_ENV}"
        },
      }
    }
  }
}
