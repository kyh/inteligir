{
  "name": "@repo/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsc",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false",
    "drizzle:kit": "pnpm with-env drizzle-kit",
    "push": "pnpm with-env drizzle-kit push",
    "push:local": "pnpm with-env drizzle-kit push --config drizzle.config.local.ts",
    "push:remote": "dotenv -e ../../.env.production.local -- drizzle-kit push",
    "studio": "pnpm with-env drizzle-kit studio",
    "with-env": "dotenv -e ../../.env --",
    "generate:auth-schema": "pnpm with-env pnpx @better-auth/cli@1.4.21 generate --config ../api/src/auth/auth.ts --output ./src/drizzle-schema-auth.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "@kyh/tsconfig": "catalog:",
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.10",
    "typescript": "catalog:"
  }
}
