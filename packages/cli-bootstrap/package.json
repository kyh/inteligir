{
  "name": "@repo/cli-bootstrap",
  "version": "0.1.0",
  "private": true,
  "description": "Generic CLI provisioning for node hosts, no workspace deps: checksum-verified GitHub-release binary install (tarball or raw), resource seeding (files/dirs/PATH), and a hardened execFile runner. Consumed by the agent's extension bundles and the connectors daemon; knows nothing about pi or the app.",
  "type": "module",
  "exports": {
    "./install": "./src/install.ts",
    "./run-cli": "./src/run-cli.ts",
    "./seed": "./src/seed.ts"
  },
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "test": "vitest run",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
