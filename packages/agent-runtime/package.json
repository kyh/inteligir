{
  "name": "@repo/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Agent provider runtime vendored from bb, trimmed to the codex app-server adapter: newline JSON-RPC over stdio, provider process lifecycle, dual-id thread stamping, event translation into the runtime's provider grammar, and interactive (approval) requests. Consumers say \"start a thread, run a turn, give me events\" — they never touch processes or wire formats.",
  "type": "module",
  "exports": {
    "./acp/acp-event-mapping": "./src/acp/acp-event-mapping.ts",
    "./acp/acp-permission-mapping": "./src/acp/acp-permission-mapping.ts",
    "./acp/acp-runtime": "./src/acp/acp-runtime.ts",
    "./acp/harness-registry": "./src/acp/harness-registry.ts",
    "./types": "./src/types.ts",
    "./vocabulary/provider-event": "./src/vocabulary/provider-event.ts",
    "./test-support/fake-acp-agent": "./src/test-support/fake-acp-agent.mjs"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/domain": "workspace:*",
    "@zed-industries/agent-client-protocol": "0.4.5",
    "@zed-industries/claude-code-acp": "catalog:",
    "@zed-industries/codex-acp": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
