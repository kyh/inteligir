// the runtime stamps it on every agent shell and the CLI reads it back there, two workspaces that
// must agree on one name: a drifted spelling leaves every agent-run verb signed as the user.
export const THREAD_ID_ENV_VAR = "INTELIGIR_THREAD_ID";
