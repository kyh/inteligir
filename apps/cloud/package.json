{
  "name": "@repo/cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "clean": "git clean -xdf .cache .turbo .wrangler dist node_modules",
    "cf-typegen": "wrangler types",
    "db:push": "dotenv -e ../../.env.production.local -- drizzle-kit push",
    "db:push:local": "drizzle-kit push --config drizzle.config.local.ts --force",
    "db:studio": "dotenv -e ../../.env.production.local -- drizzle-kit studio",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/notes": "workspace:*",
    "better-auth": "catalog:",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "catalog:",
    "@libsql/client": "^0.17.4",
    "drizzle-kit": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "catalog:"
  }
}
